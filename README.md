Todo list

Basic todo list

Installation
1 In the terminal type "npm install" to install packages.
2 In the terminal type "npm run dev" to start local server.
3 In a browser go to the address "http://localhost:5173/"